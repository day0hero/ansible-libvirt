---
- name: Install CLI tools (oc, openshift-install, oc-mirror)
  ansible.builtin.include_tasks: cli_tools.yml

- name: Install Quay mirror registry
  ansible.builtin.include_tasks: mirror_registry.yml

- name: Configure pull secret
  ansible.builtin.include_tasks: pull_secret.yml

- name: Mirror OCP content with oc-mirror v2
  ansible.builtin.include_tasks: mirror_content.yml

- name: Generate install-config.yaml
  ansible.builtin.include_tasks: install_config.yml
